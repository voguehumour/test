---
title: Search that understands the question
client: Healthcare platform
year: "2024"
image: /work/atlas.svg
---

<Lede>
Clinicians couldn't find anything in a decade of notes. We built retrieval that reasons
over structure, not just embeddings — and a UI that shows its sources so a doctor can
trust an answer in ten seconds.
</Lede>

Semantic search alone returned plausible nonsense. The fix was not a bigger embedding
model; it was respecting the structure already in the data — encounter, problem, plan —
and letting the model reason across it rather than blending everything into one vector
soup.

## Trust is a UI problem

A clinician will not act on an answer they can't verify. So every result surfaces the
exact note it came from, highlighted, one tap away. The model's confidence is shown, not
hidden, and a low-confidence answer says so plainly.

<Metrics items={[
  { value: "4.2", label: "× faster to answer" },
  { value: "92", label: "% answers cited correctly" },
  { value: "8", label: "pilot teams in week one" }
]} />

<Pull>A doctor will not act on an answer they can't verify.</Pull>

The pilot spread by word of mouth inside a week — the clearest signal that the thing was
genuinely useful, not just impressive in a demo.
